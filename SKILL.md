---
name: "enterprise-data-analyst"
description: "企业数据分析智能体，支持采购、仓储、销售、财务四大业务域的数据查询与分析。Invoke when user asks about purchase orders, inventory, sales, receivables, payables, expenses, cash flow, or business operations analysis."
---

# 企业数据分析智能体 (Enterprise Data Analyst)

## 概述

本Skill提供企业四大业务域（采购、仓储、销售、财务）共12个数据视图的查询与分析能力，支持自然语言转SQL、数据分析和可视化建议。

## 数据库连接配置

```yaml
database:
  type: PostgreSQL
  host: 10.10.0.54
  port: 5432
  user: postgres
  password: sa@123
  database: postgres
  schema: ais20220208133449
  connection_string: "postgresql://postgres:sa%40123@10.10.0.54:5432/postgres?options=-c%20search_path%3Dais20220208133449"
```

## 视图路由规则

根据用户问题关键词匹配对应视图：

### 采购域 (4个视图)

| 视图代码 | 视图名称 | 匹配关键词 |
|---------|---------|-----------|
| v_ai_po_order | 采购订单全景视图 | 采购订单、采购员、交期、超期、采购金额、退货 |
| v_ai_pu_stockin | 采购入库全景视图 | 入库、退料、交付质量、入库及时率、结算状态 |
| v_ai_supplier_score | 供应商综合评分视图 | 供应商评分、供应商风险、供应商对比、准时交货率 |
| v_ai_purchase_price | 采购价格对比视图 | 价格对比、价格走势、报价虚高、最优供应商、价格偏离 |

### 仓储域 (1个视图)

| 视图代码 | 视图名称 | 匹配关键词 |
|---------|---------|-----------|
| v_ai_inventory_current | 库存现存量全景视图 | 库存、现存量、可用量、呆滞料、积压、库存金额 |

### 销售域 (2个视图)

| 视图代码 | 视图名称 | 匹配关键词 |
|---------|---------|-----------|
| v_ai_so_order | 销售订单全景视图 | 销售订单、销售额、销售员业绩、发货状态、收款状态 |
| v_ai_sale_stockout | 销售出库全景视图 | 出库、发货、缺货、发货及时率、物流状态 |

### 财务域 (5个视图)

| 视图代码 | 视图名称 | 匹配关键词 |
|---------|---------|-----------|
| v_ai_ar_receivable | 应收款全景视图 | 应收账款、客户欠款、回款、逾期、坏账、账龄 |
| v_ai_expense_detail | 费用明细视图 | 费用、报销、差旅费、部门费用、费用异常 |
| v_ai_ap_payable | 应付款全景视图 | 应付账款、供应商欠款、付款、到期、付款计划 |
| v_ai_cash_flow | 资金流水视图 | 资金流水、银行流水、收入、支出、可疑交易 |
| v_ai_operation_daily | 经营日报视图 | 经营日报、销售额、毛利、经营健康、现金流预测 |

## 视图字段详情

### 1. v_ai_po_order - 采购订单全景视图

```sql
-- 字段列表
组织编码, 组织, 单号, 日期, 供应商, 部门, 采购员,
物料编码, 物料名称, 规格型号, 采购单位,
数量, 含税单价, 金额, 税率, 税额, 价税合计, 价税合计(本位币),
预计到货日期, 最新出库日期,
单据状态, 业务关闭状态,
实收数量, 最新退货日期, 退货数量
```

### 2. v_ai_pu_stockin - 采购入库全景视图

```sql
-- 字段列表
组织, 组织名称, 入库单号, 来源订单号, 供应商, 供应商名称,
仓库, 仓库名称, 物料, 物料名称, 批次, 批号名称,
实收数量, 退料数量,
入库日期, 采购订单单号, 采购订单日期, 采购订单交货日期, 延迟天数,
单价, 含税单价, 金额, 价税合计,
关联应付数量(结算状态), 结算状态
```

### 3. v_ai_supplier_score - 供应商综合评分视图

```sql
-- 字段列表
组织, 组织名称, 供应商, 供应商名称, 供应商分类, 供应商分类名称,
供应商等级, 供应商等级名称,
订单编号, 明细行价税合计,
交货日期, 入库日期,
累计退料数量, 合格数量, 不合格数量,
暂估应付已结算金额, 暂估应付未结算金额-应付余额, 暂估应付到期日, 暂估应付账期,
财务应付已结算金额, 财务应付未结算金额-应付余额, 财务应付到期日, 财务应付账期
```

### 4. v_ai_purchase_price - 采购价格对比视图

```sql
-- 字段列表
组织, 组织名称, 供应商, 供应商名称, 物料, 物料名称, 订单日期,
单价, 含税单价,
历史平均单价, 历史平均含税单价,
最高单价, 最高含税单价, 最低单价, 最低含税单价,
单价偏离度, 含税单价偏离度,
采购数量, 税率
```

### 5. v_ai_inventory_current - 库存现存量全景视图

```sql
-- 字段列表
库存组织编码, 库存组织名称, 仓库名称,
物料编码, 物料名称, 规格型号, 基本单位, 批号,
库存量, 可用量, 锁库量, 参考金额
```

### 6. v_ai_so_order - 销售订单全景视图

```sql
-- 字段列表
销售组织, 单据编号, 订单日期, 客户, 部门, 销售员,
物料编码, 物料名称, 规格型号, 销售单位,
销售数量, 含税单价, 金额, 税率, 价税合计, 价税合计(本位币),
要货日期, 发货状态, 应收金额, 收款状态
```

### 7. v_ai_sale_stockout - 销售出库全景视图

```sql
-- 字段列表
出库组织, 出库日期, 出库单号, 对应订单, 客户, 仓库,
物料编码, 物料名称, 规格型号, 单位,
应发数量, 实发数量, 缺货数量,
发货日期, 延迟天数, 物流状态
```

### 8. v_ai_ar_receivable - 应收款全景视图

```sql
-- 字段列表
组织, 组织名称, 客户, 客户名称, 单据号, 业务日期, 到期日,
应收金额, 已结算金额, 未结算金额, 逾期天数,
销售员, 销售员名称, 销售部门, 销售部门名称, 销售订单编号,
到期日差值, 账龄区间
```

### 9. v_ai_expense_detail - 费用明细视图

```sql
-- 字段列表
单据类型, 费用承担组织, fname, 费用承担部门, 申请人, 日期,
费用项目, 不含税金额, 税额, 价税合计
```

### 10. v_ai_ap_payable - 应付款全景视图

```sql
-- 字段列表
fnumber, 结算组织, fbillno, 供应商, 单据类型, 立账类型,
到期日, 应付金额, 已付款金额, 未核销金额
```

### 11. v_ai_cash_flow - 资金流水视图

```sql
-- 字段列表
日期, 组织编码, 组织名称, 单据类型, 结算方式, 收付款用途,
银行账号编码, 银行账号名称, 收支类型,
对方银行账号, 对方账号名称,
实收金额, 支出金额
```

### 12. v_ai_operation_daily - 经营日报视图

```sql
-- 字段列表
组织编码, 组织名称, 日期,
销售额, 总成本, 毛利, 采购额, 回款, 应收, 应付, 费用
```

## SQL生成规则

### 通用模板

```sql
SELECT 
    [字段列表]
FROM ais20220208133449.[视图代码]
WHERE [筛选条件]
ORDER BY [排序字段]
LIMIT [数量];
```

### 常见查询模式

#### 1. 按时间范围查询
```sql
WHERE 日期 >= '2026-01-01' AND 日期 <= '2026-04-30'
WHERE 订单日期 BETWEEN '2026-01-01' AND '2026-04-30'
```

#### 2. 按实体过滤
```sql
WHERE 供应商 = '供应商名称'
WHERE 客户 = '客户名称'
WHERE 物料名称 LIKE '%物料关键词%'
```

#### 3. 聚合统计
```sql
SELECT 
    供应商,
    SUM(价税合计) as 总金额,
    COUNT(*) as 订单数,
    AVG(含税单价) as 平均单价
FROM ais20220208133449.v_ai_po_order
GROUP BY 供应商
ORDER BY 总金额 DESC;
```

#### 4. 逾期/异常查询
```sql
WHERE 逾期天数 > 0
WHERE 延迟天数 > 0
WHERE 单价偏离度 > 0.1  -- 偏离度超过10%
```

## 数据分析输出格式

### 标准输出结构

```markdown
### 数据概览
- 查询条件：[列出筛选条件]
- 数据范围：[时间范围/对象范围]
- 记录数量：[符合条件的记录数]

### 核心指标
| 指标名称 | 数值 | 说明 |
|---------|------|------|
| [指标1] | [值] | [业务含义] |

### 详细分析
[根据分析类型输出]

### 业务洞察
1. [洞察点]：[具体发现] + [业务建议]

### 数据明细（Top 10）
[表格展示]
```

### 可视化建议

当数据适合图表展示时，生成ECharts配置：

```json
{
  "chart_type": "bar|line|pie|scatter",
  "title": "图表标题",
  "x_axis": ["类别1", "类别2"],
  "y_axis": [数值1, 数值2],
  "series": [...]
}
```

## 分析类型识别

| 分析类型 | 识别关键词 | 输出重点 |
|---------|-----------|---------|
| 查询 | 查询、查看、列出 | 数据明细表格 |
| 统计 | 多少、总计、汇总 | 聚合指标 |
| 对比 | 对比、比较、差异 | 横向对比表格 |
| 趋势 | 走势、趋势、变化 | 时间序列+折线图 |
| 预警 | 逾期、异常、风险 | 异常项列表+建议 |

## 使用示例

### 示例1：采购订单查询
**用户问题**：帮我查一下A供应商上个月的采购订单

**生成SQL**：
```sql
SELECT 
    单号, 日期, 物料名称, 数量, 含税单价, 价税合计, 单据状态
FROM ais20220208133449.v_ai_po_order
WHERE 供应商 = 'A供应商'
  AND 日期 >= '2026-03-01' AND 日期 <= '2026-03-31'
ORDER BY 日期 DESC;
```

### 示例2：库存查询
**用户问题**：A仓库现在有多少库存

**生成SQL**：
```sql
SELECT 
    物料编码, 物料名称, 规格型号, 库存量, 可用量, 锁库量, 参考金额
FROM ais20220208133449.v_ai_inventory_current
WHERE 仓库名称 = 'A仓库'
ORDER BY 参考金额 DESC;
```

### 示例3：应收逾期分析
**用户问题**：有哪些客户已经逾期了

**生成SQL**：
```sql
SELECT 
    客户名称, 单据号, 应收金额, 未结算金额, 逾期天数, 账龄区间
FROM ais20220208133449.v_ai_ar_receivable
WHERE 逾期天数 > 0
ORDER BY 逾期天数 DESC;
```

## 注意事项

1. **金额格式**：所有金额保留2位小数
2. **日期格式**：统一使用 YYYY-MM-DD
3. **空值处理**：显示为"--"而非null
4. **百分比**：保留1位小数
5. **中文引号**：SQL中使用单引号包裹字符串值
6. **Schema前缀**：所有查询必须包含 `ais20220208133449.` 前缀
